spring:
  cloud:
    consul:
      discovery:
        prefer-ip-address: true
        ip-address: 192.168.65.2
#        hostname: ${GATEWAY_HOSTNAME}

        port: ${GATEWAY_PORT}
        healthCheckPath: /health
      host: ${GATEWAY_CONSUL_HOST}
      port: ${GATEWAY_CONSUL_PORT}
      enabled: true