spring:
  cloud:
    consul:
      discovery:
#        prefer-ip-address: true
#        ip-address: 192.168.65.2
        hostname: gateway

        port: 8080
        healthCheckPath: /health
      host: ${CONSUL_HOST}
      port: ${CONSUL_PORT}
      enabled: true